This is a Networking with C++, improoved version. In last Repository The server side wasn't handling more than one client at a time. In this version, It has multithreading features. For each client program will create a new thread, and detach it from the main thread, meaning that it will execute independantly.

Next step is an Android version of client, written in java. It will do a simple Job, like blink a light from Mobile phone or something Similar...
